
# See the docstring in versioneer.py for instructions. Note that you must
# re-run 'versioneer.py setup' after changing this section, and commit the
# resulting files.

[versioneer]
VCS = git
style = pep440-post
versionfile_source = src/alqtendpy/_version.py
versionfile_build = alqtendpy/_version.py
tag_prefix =
